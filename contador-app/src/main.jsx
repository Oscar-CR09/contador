import React from 'react'
import ReactDOM from 'react-dom/client'
import { ConterApp } from './ConterApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ConterApp />
  </React.StrictMode>,
)
